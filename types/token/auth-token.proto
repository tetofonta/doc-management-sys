message TokenPayload{
  required string user_id = 1;
  required bool superuser = 2;
  repeated string features = 3;
  repeated string groups = 4;
  required string user_name = 5;

  optional string sibling_hash = 100;
}